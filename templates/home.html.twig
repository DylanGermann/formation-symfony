{% extends "base.html.twig" %}

    {% block title %}Bienvenue sur le meilleur réseau d'appartements{% endblock %}

    {% block header %}
    <h1>Voici le header</h1>
    {% endblock %}

    {% block body %}
        <div class="container">
            <div class="p-5 mb-4 bg-light rounded-3 mt-3">
                <div class="container-fluid py-5">
                    <h1 class="display-5 fw-bold">Bienvenue sur SymBNB</h1>
                    <p class="col-md-8 fs-4">Nous mettons en relation les voyageurs et propriétaires qui veulent profiter d'opportunités et gagner de l'argent en proposant leur bien à la location</p>
                    <hr>
                    <p>Vous êtes voyageur ? Et vous voulez voir ou vous pourrez loger pour votre prochain déplacement ?</p>
                    <a href="{{ path('ads_index') }}"><button class="btn btn-primary btn-lg" type="button">Voir nos annonces</button></a>
                </div>
            </div>
            <h2 class="h1 mt-3 mb-4">Nos appartements stars</h2>
            <div class="row">
                {% for data in ads %}
                    {% include "ad/ad.html.twig" with {'ad': data.annonce} %}
                {% endfor %}
            </div>
            <h2 class="h1 my-5">Nos propriétaires stars !</h2>
            <div class="row">
            {% for data in users %}
                <div class="col">
                    <div class="card border-secondary mb-3">
                        <div class="card-body">
                            <h4 class="card-title">
                                {{data.user.fullName}}
                            </h4>
                            <hr>
                            <div class="card-text">
                                <img src="{{data.user.picture}} " alt="Avatar de {{data.user.fullName}} " class="float-start me-3">
                                <p>{{data.user.introduction}}</p>
                                <a href="{{path('user_show', {'slug' : data.user.slug})}}" class="btn btn-success float-end">En savoir plus!</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    {% endblock %}
    